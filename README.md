# chatbot
A tf-idf algorithm based chatbot based on Movie-Corpus.
 You can change the datset for better results and even give the chatbot a personality but since it 
works on a retrieval algorithm so don't put a very large dataset or you may get very slow results
